import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.Random;

public class Main {

    public static void main(String[] args) {
        Random rnd = new Random();
        //Dog dog1 = new Dog("Mars", 47.3);
        List<Dog> dolmatains = null;
        for (int i = 0; i < 40; i++) {
            if (dolmatains == null || dolmatains.size() == 0) {
                Dog[] names = {new Dog("Mars", 2), new Dog("Petya", 4), new Dog("Freddy", 3)new Dog("Red", 5);
                dolmatains = new ArrayList<>(Arrays.asList(names));
         

        }


            }
        }
    }
